; npm scripts (build/build:prod/build:dev) use bash-style `NODE_ENV=x cmd`
; syntax, which cmd.exe can't parse. Route npm's script execution through
; Git Bash instead so `npm run build` works as-is on Windows.
script-shell=C:\\Program Files\\Git\\usr\\bin\\bash.exe
