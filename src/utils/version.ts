declare const __CLI_VERSION__: string;

export const CLI_VERSION: string = __CLI_VERSION__;

// The upstream dust-tt/dust cli/dust-cli version this fork was last synced
// against - see AGENTS.md's "Last synced" line, which it must be kept in
// step with (update both together after every sync).
export const UPSTREAM_CLI_VERSION = "0.4.5";
